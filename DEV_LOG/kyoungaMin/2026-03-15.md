# 개발일지 — 2026-03-15 (Day 10)

> **작성자**: kyoungaMin
> **프로젝트**: 반도체 부품·소재 수요예측 AI SaaS

---

## 작업 목표
- 분석 리포트 2종(zero_target, top_error) 정확도 검토 및 수정
- 오차 상위 제품 정보 별도 제공 기능 구현 (예측 신뢰도 배지 + 주의 제품 패널)

## 작업 내역

| 시간 | 작업 내용 | 카테고리 | 상태 |
|------|----------|----------|------|
| AM | `zero_target_analysis_report.md` 코드 교차 검증 및 오류 4건 수정 | `문서` | `완료` |
| AM | `top_error_analysis_report.md` 내부 논리 교차 검증 및 오류 2건 수정 | `문서` | `완료` |
| PM | `WeeklyForecast.tsx` — `ForecastConfidenceBadge` 컴포넌트 추가 | `프론트` | `완료` |
| PM | `RiskManagement.tsx` — `HighUncertaintyPanel` 컴포넌트 추가 | `프론트` | `완료` |
| PM | `/api/forecast-weekly/confidence/route.ts` 신규 API 라우트 생성 | `백엔드` | `완료` |
| PM | 관련 문서 전체 업데이트 (README, DEV_LOG.md) | `문서` | `완료` |

> **카테고리**: `세팅` `데이터` `모델링` `백엔드` `프론트` `인프라` `테스트` `문서` `리팩토링` `버그수정` `회의`

## 산출물

### 리포트 수정 (DEV_LOG/)
- `DEV_LOG/zero_target_analysis_report.md` — 오류 4건 수정
  - `96개 피처` → `69개 피처` (실제 WEEKLY_FEATURE_COLS 기준)
  - `target_3w` 누락 행 추가 (코드에 있으나 리포트에서 누락)
  - `target_4w` 계산식 수정: `target_2w + shift(-3) + shift(-4)` → `target_3w + shift(-4)`
  - `zero_ratio_13w` 설명 수정: `비영주 비율` → `수요 없는 주(zero) 비율`
- `DEV_LOG/top_error_analysis_report.md` — 오류 2건 수정
  - A010003 시계열 실제값 수정: `W07 | 1,112` → `W07 | 1,412` (오차 계산 기준)
  - A010029 유형 재분류: 유형 D(수요 붕괴) → 유형 E(자연 분산 범위 내 오차)로 분리

### 신규 기능 (프론트엔드)
- `forecastai/src/app/api/forecast-weekly/confidence/route.ts` — 예측 신뢰도 낮은 제품 조회 API
  - `feature_store_weekly`에서 제품별 최신 주차 기준 `zero_ratio_13w`, `cv4` 계산
  - 점수 = zeroRatio×0.6 + cv기여×0.4 → 상위 N개 반환
- `forecastai/src/components/pages/WeeklyForecast.tsx` — `ForecastConfidenceBadge` 추가
  - 예측 rows(P10~P90 밴드)의 평균 폭 비율로 신뢰도 3단계 산출
  - low(≥1.5) / medium(0.7~1.5) / good(<0.7), LIVE 데이터 시에만 표시
- `forecastai/src/components/pages/RiskManagement.tsx` — `HighUncertaintyPanel` 추가
  - `/api/forecast-weekly/confidence` 연동, 6개 제품 카드 그리드
  - 접기/펼치기 토글, 로딩 스켈레톤, DB 미응답 시 자동 숨김

## 이슈/블로커
- 없음

## 의사결정 기록

| 결정 사항 | 선택지 | 결정 | 사유 |
|-----------|--------|------|------|
| 신뢰도 배지 계산 기준 | ① meta.coverageRate(과거) ② 밴드폭 비율(미래) | 밴드폭 비율 | 과거 정확도와 미래 불확실성은 다른 개념, 현재 예측의 불확실성을 직접 표시 |
| 주의 제품 데이터 출처 | ① 정적 하드코딩 ② 새 API(feature_store_weekly) ③ model_evaluation WMAPE | feature_store_weekly | 실시간 반영, 가장 최신 데이터 기반, 추가 백엔드 파이프라인 불필요 |
| 주의 제품 UI 위치 | ① 별도 페이지 ② WeeklyForecast 배지 ③ RiskManagement 패널 ④ ②+③ 병행 | ②+③ 병행 | 예측 맥락과 리스크 맥락 양쪽에서 정보 제공, 클릭 없이 바로 확인 |

## 내일 할 일
- [ ] 신뢰도 배지 실데이터 연결 후 임계값 조정 검토 (low/medium 경계 1.5/0.7이 적절한지 확인)
- [ ] Phase 6 통합 테스트 시작 (E2E 기능 테스트)

## 메모
- `tsc --noEmit` 0 errors 확인
- zero_target 리포트의 섹션 6 다이어그램에 `96개 피처` 잔존 → README와 함께 일괄 수정 완료
- 피처 수 확정: WEEKLY_FEATURE_COLS = 69개 (기본), v4 add_product_features 추가 5개 → v4 학습 시 74개
