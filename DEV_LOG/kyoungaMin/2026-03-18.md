# 개발일지 — 2026-03-18 (Day 19)

> **작성자**: kyoungaMin
> **프로젝트**: 반도체 부품·소재 수요예측 AI SaaS

---

## 작업 목표
- 모니터링/알림 시스템 전체 구현 (API 에러율, DB 연결 상태, 파이프라인 실행 결과)
- ML 파이프라인 배치 자동화 (주 1회 S0→S8 전체 실행)
- 브랜치 통합 (dev/jieun 머지 + origin/main 동기화)

## 작업 내역

| 시간 | 작업 내용 | 카테고리 | 상태 |
|------|----------|----------|------|
| - | 브랜치 변경사항 확인 + dev/jieun 머지 + push | `세팅` | `완료` |
| - | Supabase 테이블 3개 생성 (system_alert, system_health_log, api_log) | `인프라` | `완료` |
| - | /api/health 헬스체크 엔드포인트 구현 | `백엔드` | `완료` |
| - | /api/monitoring/alerts 알림 CRUD 구현 (GET/POST/PATCH) | `백엔드` | `완료` |
| - | /api/monitoring/evaluate-rules 자동 규칙 평가 엔진 구현 | `백엔드` | `완료` |
| - | /api/monitoring/health-logs 헬스체크 이력 조회 API | `백엔드` | `완료` |
| - | /api/monitoring/api-stats API 라우트별 에러율/응답시간 통계 | `백엔드` | `완료` |
| - | apiLogger.ts 공통 유틸 (API 로깅 + 파이프라인 실패 알림) | `백엔드` | `완료` |
| - | Monitoring.tsx 모니터링 대시보드 페이지 (4탭) | `프론트` | `완료` |
| - | 사이드바/라우팅에 모니터링 메뉴 연결 (Admin 전용) | `프론트` | `완료` |
| - | data-pipeline POST에 파이프라인 실패 시 자동 알림 연동 | `백엔드` | `완료` |
| - | 실제 Supabase DB 연동 API 통합 테스트 (10건 전체 통과) | `테스트` | `완료` |
| - | ML 배치 실행기 batch_weekly.py 구현 | `인프라` | `완료` |
| - | Windows 배치 파일 run_batch.bat 생성 | `인프라` | `완료` |
| - | DataPipelineManager에 ML 배치 파이프라인 항목 추가 | `프론트` | `완료` |
| - | Help.tsx 도움말 센터 페이지 구현 (사용자 매뉴얼 / API 안내 / 운영 매뉴얼 3탭) | `프론트` | `완료` |
| - | 사이드바·라우팅에 도움말 메뉴 연결 (전 역할 접근 가능) | `프론트` | `완료` |

> **카테고리**: `세팅` `데이터` `모델링` `백엔드` `프론트` `인프라` `테스트` `문서` `리팩토링` `버그수정` `회의`

## 산출물

### DB 마이그레이션
- `DB/migrations/create_monitoring_tables.sql` — system_alert, system_health_log, api_log 3개 테이블

### API 엔드포인트 (6개 신규)
- `/api/health` — DB 연결 + 파이프라인 + 알림 종합 상태 체크
- `/api/monitoring/alerts` — 알림 조회/생성/읽음처리/해제 (GET/POST/PATCH)
- `/api/monitoring/evaluate-rules` — 자동 규칙 점검 (파이프라인 실패, DB 장애, API 에러율)
- `/api/monitoring/health-logs` — 헬스체크 이력 조회
- `/api/monitoring/api-stats` — API 라우트별 에러율/응답시간 통계

### 공통 유틸
- `forecastai/src/lib/apiLogger.ts` — API 로그 기록 + 파이프라인 실패 알림 함수

### 프론트엔드
- `forecastai/src/components/pages/Monitoring.tsx` — 4탭 (시스템 상태 / 알림 / API 현황 / 체크 이력)
- `forecastai/src/components/pages/Help.tsx` — 도움말 센터 3탭 (사용자 매뉴얼 / API 안내 / 운영 매뉴얼)

### ML 배치 자동화
- `DB/07_pipeline/batch_weekly.py` — S0→S8 배치 실행기 (DB 기록 + 실패 알림)
- `DB/07_pipeline/run_batch.bat` — Windows 작업 스케줄러 등록용 배치 파일

## 이슈/블로커
- Next.js dev 서버 `.next` 캐시로 인해 health-logs API가 최신 데이터를 반환하지 않는 현상 → `.next` 폴더 삭제 후 재시작으로 해결

## 의사결정 기록

| 결정 사항 | 선택지 | 결정 | 사유 |
|-----------|--------|------|------|
| 모니터링 구현 방식 | 외부 서비스(Sentry/DataDog) / 자체 구현 | 자체 구현 | Vercel+Supabase 스택 내에서 비용 없이 구현 가능 |
| 알림 중복 방지 | 시간 윈도우 / 키 기반 / 없음 | 6시간 윈도우 | 동일 제목 알림의 과다 생성 방지, 운영 피로도 감소 |
| ML 배치 실행 방식 | Vercel Cron / Windows 작업 스케줄러 / 수동 | Windows 작업 스케줄러 | Python ML 파이프라인은 서버에서 직접 실행 필요 (Vercel 불가) |
| 배치 결과 기록 | 로그 파일만 / DB만 / 둘 다 | 둘 다 | 파일 로그는 디버깅용, DB 기록은 대시보드 표시용 |

## 다음 세션 할 일
- [ ] 백업 전략 — Supabase DB 정기 백업 설정
- [x] ~~사용자 매뉴얼 — 주요 기능별 사용 가이드~~ → Help.tsx 도움말 센터로 통합 구현
- [x] ~~API 문서 — 53개 API 정리~~ → Help.tsx API 안내 탭으로 통합 구현
- [ ] E2E 기능 테스트 — 18개 페이지 전체 시나리오

## 메모
- 모니터링 시스템은 테이블이 이미 Supabase에 생성되어 있어 마이그레이션 별도 실행 불필요
- ML 배치는 `run_batch.bat`을 Windows 작업 스케줄러에 매주 월요일 06:00으로 등록하면 자동화 완료
