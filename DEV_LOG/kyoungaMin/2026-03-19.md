# 개발일지 — 2026-03-19 (Day 15)

> **작성자**: kyoungaMin (kamin@penta.co.kr)
> **프로젝트**: 반도체 부품·소재 수요예측 AI SaaS

---

## 작업 목표
- 외부지표 데이터 보강: 무료 대리지표(MU, WDC, 반도체 PPI) 추가로 DRAM/NAND 유료 데이터 대체
- 주차 표시 형식 전 페이지 통일 (공통 유틸 도입)
- 리스크관리 페이지 주간/월간 토글 + 대시보드 연동 개선
- API 성능 최적화 (인덱스 추가, 타임아웃 방지)

## 작업 내역

| 시간 | 작업 내용 | 카테고리 | 상태 |
|------|----------|----------|------|
| - | DB: forecast_result 복합 인덱스 추가 (model+product, model+target_date) | `데이터` | `완료` |
| - | 외부지표 수집기: Micron(MU), WDC 주가, 반도체 PPI(FRED) 수집 함수 추가 | `데이터` | `완료` |
| - | ext-semi API: FRED API 연동 + Yahoo Finance 대리지표 병합 로직 구현 | `백엔드` | `완료` |
| - | dashboard API: weekly용 eval_date 조회 추가 (리스크관리 주간 연동) | `백엔드` | `완료` |
| - | model-scenario API: 최근 90일 데이터만 스캔하여 타임아웃 방지 | `백엔드` | `완료` |
| - | lib/data.ts: 주차 공통 유틸 (formatWeekLabel, formatMonthLabel 등) 추가 | `프론트` | `완료` |
| - | Dashboard, ActionQueue, Purchase, ExecutiveReport, ModelEvaluation: 주차 표시 공통 포맷 적용 | `프론트` | `완료` |
| - | ExternalIndicators: MU/WDC/PPI 대리지표 티커·차트·테이블 표시 | `프론트` | `완료` |
| - | RiskManagement: 기본 주간 모드 + 대시보드→리스크 주간 연동 | `프론트` | `완료` |
| - | DataPipelineManager: 주차 헬퍼를 공통 유틸로 교체 | `리팩토링` | `완료` |
| - | externalData.ts: MU_CLOSE, WDC_CLOSE, SEMI_PPI 메타 추가 | `프론트` | `완료` |
| - | ai-indicator-insight API: 반도체 산업지표에 MU/WDC/PPI 키 추가 | `백엔드` | `완료` |

## 산출물
- DB: `06_analytics_ddl.sql` — 복합 인덱스 2개 추가
- 수집기: `15_load_realtime_indices.py` — MU, WDC, SEMI_PPI 수집 함수 + 레지스트리 등록
- API: `ext-semi/route.ts` — FRED API 연동, 대리지표 병합 응답
- API: `dashboard/route.ts` — weeklyEvalDate 필드 추가
- API: `model-scenario/route.ts` — 90일 컷오프 적용
- 프론트: `lib/data.ts` — getMonday, getWeekOfMonth, formatWeekLabel, formatMonthLabel, formatPeriodLabel
- 프론트: Dashboard, ActionQueue, Purchase, ExecutiveReport, ModelEvaluation, RiskManagement, DataPipelineManager, ExternalIndicators 수정

## 이슈/블로커
- DRAM/NAND 현물가 데이터는 TrendForce 유료 API 의존 → Micron(MU), WDC 주가를 무료 대리지표로 활용하여 우회
- FRED API Key 미설정 시 반도체 PPI 수집 불가 (비필수, graceful 처리)

## 의사결정 기록

| 결정 사항 | 선택지 | 결정 | 사유 |
|-----------|--------|------|------|
| DRAM/NAND 유료 데이터 대체 | (A) 유료 계약 (B) 무료 대리지표 | (B) MU/WDC 주가 + PPI | 무료 API로 메모리 가격 추세 대리 가능, MU 매출의 90%+ 메모리 의존 |
| 주차 표시 형식 | (A) 각 페이지별 개별 구현 유지 (B) 공통 유틸 통합 | (B) lib/data.ts 공통 유틸 | 6개+ 페이지에서 중복 구현 존재, 일관성·유지보수 위해 통합 |
| 리스크관리 기본 모드 | (A) 월간 (B) 주간 | (B) 주간 | 대시보드가 주간 단위이므로 연동 시 주간이 자연스러움 |

## 내일 할 일
- [ ] FRED API Key 환경변수 설정 및 PPI 데이터 수집 테스트
- [ ] 대리지표 vs 실제 메모리 가격 상관관계 검증
- [ ] E2E 테스트 (외부지표 페이지 대리지표 표시 확인)

## 메모
- Micron(MU)은 DRAM/NAND 매출 비중 90% 이상 → 메모리 현물가와 높은 상관관계
- FRED PCU33443344 = 반도체 생산자물가지수 (Producer Price Index) — 월간, 무료
- ext-semi API fallback 순서: DB(1순위) → Yahoo Finance + FRED(2순위)
