# 개발일지 — 2026-03-06 (Day 4)

> **작성자**: kyoungaMin (kamin@penta.co.kr)
> **프로젝트**: 반도체 부품·소재 수요예측 AI SaaS

---

## 작업 목표
- 최적화 > 생산 권고(ActionQueue) 페이지 전면 개편
- Supabase 실데이터 연동

## 작업 내역

| 시간 | 작업 내용 | 카테고리 | 상태 |
|------|----------|----------|------|
| - | data.ts에 S7 파이프라인 기반 생산 권고 Mock 데이터 추가 (PRODUCTION_PLAN_DATA 10개 SKU, PRODUCTION_LINE_CHART, PROD_PRIORITY_DIST, PLAN_TYPE_LABELS, PRIORITY_STYLE) | `프론트` | `완료` |
| - | ActionQueue.tsx 전면 재작성: KPI 4종, 주간 라인별 Stacked Bar Chart, 우선순위 Pie Chart, 계획유형 분포 바, 상세 테이블(14컬럼), 우측 Detail Drawer(비교 차트+AI 근거+수량 조정), 승인/수정 워크플로우, AI 채택률 | `프론트` | `완료` |
| - | @supabase/supabase-js 설치 및 Supabase 서버 클라이언트 설정 (lib/supabase.ts) | `백엔드` | `완료` |
| - | Next.js API route 생성: GET /api/production-plan (production_plan + product_master + risk_score + daily_production + daily_order + product_lead_time 조인), PATCH /api/production-plan (상태 변경) | `백엔드` | `완료` |
| - | ActionQueue.tsx 실데이터 연동: API fetch → Mock fallback, 동적 필터 옵션, 새로고침 버튼, 데이터 소스 배지(DB/Mock), 승인 시 DB PATCH | `프론트` | `완료` |
| - | layout/index.tsx Set 이터레이션 TypeScript 에러 수정 (Array.from) | `버그수정` | `완료` |

## 산출물
- `forecastai/src/lib/data.ts` — Mock 데이터 확장 (PRODUCTION_PLAN_DATA 외 5개 export 추가)
- `forecastai/src/lib/supabase.ts` — Supabase 서버 클라이언트 (신규)
- `forecastai/src/app/api/production-plan/route.ts` — API route GET/PATCH (신규)
- `forecastai/src/components/pages/ActionQueue.tsx` — 생산 권고 페이지 전면 개편 + 실데이터 연동 (~500줄)
- `forecastai/.env.local` — Supabase 환경변수 (신규)
- `forecastai/src/components/layout/index.tsx` — Set 이터레이션 버그 수정

## 이슈/블로커
- 기존 Admin.tsx의 TypeScript RoleType 호환 에러 (기존 이슈, 본 작업과 무관)

## 의사결정 기록

| 결정 사항 | 선택지 | 결정 | 사유 |
|-----------|--------|------|------|
| 기존 ACTION_ITEMS_FULL 유지 여부 | 삭제 vs 유지 | 유지 | Dashboard.tsx에서 참조 중이므로 기존 데이터 유지, 신규 PRODUCTION_PLAN_DATA 별도 추가 |
| Detail 패널 구현 방식 | 모달 vs 우측 Drawer | 우측 Drawer | RiskManagement.tsx 패턴과 일관성 유지, 테이블과 동시 참조 가능 |
| 실데이터 연동 방식 | 클라이언트 직접 vs API route | API route | Service Role Key 노출 방지, 여러 테이블 조인 서버사이드 처리 |
| DB 미연결 시 동작 | 에러 표시 vs Mock fallback | Mock fallback | 개발/데모 환경에서 DB 없이도 정상 동작하도록 graceful degradation |

## 내일 할 일
- [ ] 구매 권고(Purchase.tsx) 페이지 고도화 및 실데이터 연동
- [ ] 생산 권고 페이지 UI 세부 조정

## 메모
- API route는 production_plan(최신 plan_date) + product_master(이름) + risk_score(위험점수) + daily_production(라인) + daily_order(고객/수주) + product_lead_time(리드타임) 6개 테이블 조인
- dominant risk type은 stockout/excess/delivery/margin 중 최고 점수로 결정
- 승인 시 optimistic UI 업데이트 + 백그라운드 DB PATCH
- 우선순위 분포/라인 차트/필터 옵션 모두 실데이터 기반 동적 생성
