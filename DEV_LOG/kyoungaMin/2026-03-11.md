# 개발일지 — 2026-03-11 (Day 8)

> **작성자**: kyoungaMin (kamin@penta.co.kr)
> **프로젝트**: 반도체 부품·소재 수요예측 AI SaaS

---

## 작업 목표
- 멀티 모델 성능비교 결과를 활용한 구간별 모델 선택기(Segment-based Model Selector) 구현
- 브랜치 머지 + 빌드 검증 + API 오류 수정
- risk_score 테이블 eval_type 분리 + 백필 데이터 생성
- 모델 평가 UI 개선

## 작업 내역

| 시간 | 작업 내용 | 카테고리 | 상태 |
|------|----------|----------|------|
| - | dev/jieun, dev/dasom, KoraenDavid 브랜치 → main 머지 + 충돌 해결 | `세팅` | `완료` |
| - | API 8개 route에 `force-dynamic` 추가 (정적 렌더링 오류 수정) | `버그수정` | `완료` |
| - | Supabase SDK 2.27.2 호환성 패치 (SyncClient → Client 임포트) | `버그수정` | `완료` |
| - | risk_score 테이블에 eval_type 컬럼 추가 (weekly/monthly 분리) | `데이터` | `완료` |
| - | s5_risk_score.py 리팩토링 — eval_type + 백필 지원 | `모델링` | `완료` |
| - | 2026-01~02 risk_score 백필 실행 (주간 8회 + 월간 2회 = 133,077건) | `데이터` | `완료` |
| - | ModelEvaluation.tsx 조회주차 UI를 생산권고 스타일로 개선 | `프론트` | `완료` |
| - | **s4s_segment_selector.py 신규 생성** — 구간별 최적 모델 선택기 | `모델링` | `완료` |
| - | config.py에 SEGMENT_MODEL_MAP 설정 추가 (저/중/고수요 구간별 모델 매핑) | `모델링` | `완료` |
| - | s5_risk_score.py, s7_production_plan.py → segment_best_v1 우선 참조로 수정 | `모델링` | `완료` |
| - | run_pipeline.py에 4s 스텝 등록 | `모델링` | `완료` |
| - | segment_best_v1 실행 → 11,431건 적재 (주간 4,280 + 월간 7,151) | `데이터` | `완료` |
| - | SCHEMA_REFERENCE.md 업데이트 (Step 4S 문서화) | `문서` | `완료` |

> **카테고리**: `세팅` `데이터` `모델링` `백엔드` `프론트` `인프라` `테스트` `문서` `리팩토링` `버그수정` `회의`

## 산출물
- `DB/07_pipeline/s4s_segment_selector.py` — 구간별 모델 선택기 (신규)
- `DB/07_pipeline/config.py` — 세그먼트 설정 + SDK 호환 패치
- `DB/07_pipeline/s5_risk_score.py` — eval_type 지원 + segment_best_v1 우선 참조
- `DB/07_pipeline/s7_production_plan.py` — segment_best_v1 우선 참조
- `DB/07_pipeline/run_pipeline.py` — 4s 스텝 등록
- `DB/SCHEMA_REFERENCE.md` — Step 4S + risk_score eval_type 문서화
- `forecastai/src/components/pages/ModelEvaluation.tsx` — 조회주차 UI 개선

## 이슈/블로커
- Supabase SDK 2.27.2에서 `SyncClient` → `Client`로 이름 변경됨 → try/except 패치 적용
- API 정적 렌더링 시 `request.url` 참조 불가 → `force-dynamic` export 추가로 해결
- .next 캐시 손상 → `rm -rf .next` 후 클린 빌드로 해결

## 의사결정 기록

| 결정 사항 | 선택지 | 결정 | 사유 |
|-----------|--------|------|------|
| 멀티모델 활용 전략 | 앙상블 / 구간별 선택 / 용도별 분리 | 구간별 모델 선택 (방향 1) | 제품 70~80%가 저수요 구간, SVR의 ±5 정확도(62.6%)가 LightGBM(6.7%)을 압도 |
| 구간 경계값 | 5/50 / 10/100 / 20/200 | 10/100 (일평균 수주) | 모델 비교 보고서 Small(<10)/Medium(10~99)/Large(100+) 구간과 정합 |
| 구간별 모델 — 주간 | LightGBM/SVR/Ridge | 저수요=SVR, 중·고수요=LightGBM | SVR ±5=62.6%, LightGBM R²=0.27 |
| 구간별 모델 — 월간 | LightGBM/SVR/Ridge | 저수요=SVR, 중·고수요=Ridge | SVR MAE 최저, Ridge R²=0.69 |
| model_id 방식 | 개별 model_id 보존 / 통합 segment_best_v1 | segment_best_v1 통합 | 하류 파이프라인(S5~S8)이 단일 model_id 참조, 폴백 로직 포함 |
| risk_score eval_type 분리 | A안(날짜 규칙 기반) / B안(eval_type 컬럼) | B안 (eval_type 컬럼) | 주/월 날짜 충돌 시 데이터 오염 방지, UNIQUE 제약 명확 |

## 모델/알고리즘 실험 기록 (해당 시)

| 실험 ID | 모델/방법 | 파라미터 | 주요 지표 | 결과 요약 |
|---------|----------|----------|-----------|-----------|
| segment_v1 | 구간별 모델 선택기 | low<10, high>=100, lookback=90일 | 11,431건 선택 | 주간: LightGBM 2,783건 + SVR 1,497건, 월간: Ridge 3,686건 + SVR 3,465건 |

## 내일 할 일
- [ ] segment_best_v1 기반 리스크 스코어 재산출 검증
- [ ] 모델 평가 대시보드에 구간별 선택 결과 시각화 추가
- [ ] 외부 경제지표 데이터 활용 방안 검토

## 메모
- 구간 분류 결과: 저수요 303개 / 중수요 1,100개 / 고수요 718개 (총 2,121개 제품)
- 폴백 0건 → 모든 제품에 대해 최적 모델 데이터 존재 확인
- 파이프라인 실행 순서: S4 → S4L → S4m → **S4S** → S5 → S6 → S7 → S8
