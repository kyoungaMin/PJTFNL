# 개발일지 — 2026-03-12

## 오늘 작업 요약

### 헤더 알림 → 페이지 이동 연결
- `alerts/route.ts`: AlertItem에 `page?` 필드 추가
  - action_queue critical/high → `'action-queue'`
  - risk_score E/F → `'risk'`
  - purchase_order pending → `'purchase'`
- `layout/index.tsx` Header: 알림 클릭 시 `setPage(a.page)` 호출

### KPI 박스 → 페이지 이동 연결
- `Dashboard.tsx`: KpiCard에 `onNavigate` prop 추가
- `KPI_PAGE_MAP`: order → purchase, coverage → inventory, aiaction → action-queue, urgent → risk
- 호버 시 "상세 →" 화살표 표시

### 날짜 기준 통일 (핵심)

**최종 구조 — planDate = ML eval_date가 속한 주의 일요일**

| 화면 | 사용 날짜 | 결과 |
|------|-----------|------|
| 대시보드 헤더 | `planDate = '2026-02-22'` | 2월 4주차 · 2/22~2/28 |
| 위험 도넛 / AI 권고 데이터 | eval_date 2/22~2/28 범위 | 동일 주차 ✓ |
| 구매 발주 클릭 | `planDate = '2026-02-22'` | 동일 주차 ✓ |
| AI 생산 권고 클릭 | `planDate = '2026-02-22'` | 동일 주차 ✓ |
| 리스크 관리 클릭 | `planDate` → eval_date 매칭 | 동일 주차 ✓ |
| 재고 현황 클릭 | `yearMonth = '2026-02'` | 2월 데이터 ✓ |

**ML 파이프라인이 3월 1주차를 생성하면 → eval_date가 3월 1주차로 바뀌고 → 전체 화면이 자동으로 3월 1주차로 업데이트**

---

## 수정된 파일

### `forecastai/src/app/api/dashboard/route.ts`
- `planDate`: `purchase_recommendation.plan_date` 최신값
- `planDateEnd`: planDate + 6일
- risk_score: planDate 주차 내 eval_date 먼저 조회 → 없으면 최신 fallback
- action_queue: planDate 주차 내 pending 먼저 조회 → 없으면 최신 fallback
- `headerPlanDate`: `evalDataRow.eval_date`가 속한 주의 일요일 (`d.getDate() - d.getDay()`)
- `refWeekInfo.planDate = headerPlanDate` (ML 실데이터 기준)

### `forecastai/src/app/api/alerts/route.ts`
- `AlertItem` 인터페이스에 `page?: string` 추가
- 각 알림 push 시 page 값 포함

### `forecastai/src/components/layout/index.tsx`
- alertItems 타입에 `page?` 추가
- 알림 항목 클릭 시 `setPage(a.page)` + `closeAll()`
- `cursor: a.page ? "pointer" : "default"`

### `forecastai/src/components/pages/Dashboard.tsx`
- `refWeekInfo` 인터페이스에 `planDate: string` 추가
- 헤더 sub 계산: `planDate` 기준, 일요일이면 +1일(월요일)로 주차 라벨 계산
- API 응답 후 `sessionStorage.setItem('dashRefWeek', JSON.stringify(d.refWeekInfo))`
- KpiCard: `onNavigate={setPage}` 연결

### `forecastai/src/components/pages/Purchase.tsx`
- 초기 로드 시 sessionStorage `dashRefWeek`에서 `ref.planDate || ref.weekStart` 읽어 필터

### `forecastai/src/components/pages/ActionQueue.tsx`
- 동일: `ref.planDate || ref.weekStart`

### `forecastai/src/components/pages/RiskManagement.tsx`
- `dashPlanDate` → `dashWeekStart` 순으로 available dates와 매칭

### `forecastai/src/components/pages/Inventory.tsx`
- `ref.yearMonth` ('2026-02') → '202602' 변환 후 loadDashboard 호출

---

## 다음 세션 할 일
- AI 인사이트 API (OpenAI / Claude / Gemini) — 팀 결정 후 키 발급
- 비밀번호 변경: Supabase 이메일 방식 구현
