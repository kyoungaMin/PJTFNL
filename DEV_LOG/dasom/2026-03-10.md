# 2026-03-10 개발일지

> **작성자**: 김다솜 (dev/dasom)
> **브랜치**: dev/dasom

---

## 전날(03-09) 계획 → 완료 여부

| 계획 | 완료 여부 |
|------|:---:|
| 대시보드 수주량 차트 예측 밴드 — ML 팀 `forecast_result` 적재 대기 | ✅ ML 파이프라인 직접 구축으로 대체 완료 |
| `risk_score` 테이블 적재 후 위험 도넛 차트 실데이터 전환 확인 | ✅ 완료 (배지+자동 전환 구현) |
| `action_queue` 테이블 적재 후 AI 생산 권고 실데이터 전환 확인 | ✅ 완료 (배지+자동 전환+product_name 조인) |

> **방향 전환 이유**: ML 팀이 별도로 없어서 직접 ML 파이프라인을 구축하는 것으로 결정.
> 기존에 팀원이 만들어둔 `DB/07_pipeline/` 코드가 있었고, 이를 고도화하는 방식으로 진행.

---

## 오늘 한 일

### 1. ML 파이프라인 전략 수립

#### 배경
대시보드 일부 영역이 ML 결과 테이블(`forecast_result`, `risk_score`, `action_queue`)이 비어 있어 목업 상태로 표시되고 있었음.
ML 팀이 별도로 없어 직접 구현하기로 결정.

#### 선택한 방향
```
Phase 1  Rule-based SQL 함수 (완료) → 목업 → 실데이터 전환
Phase 2  LightGBM ML 파이프라인 고도화 (오늘 작업)
Phase 3  파이프라인 실행 → DB 적재 → 대시보드 자동 전환
```

#### 왜 Prophet 대신 LightGBM을 선택했나?
| 항목 | Prophet | LightGBM |
|------|---------|----------|
| 특징 | 단순 시계열 패턴 학습 | 60개 이상의 복합 피처 학습 가능 |
| 반도체 수요예측 적합성 | 낮음 (계절성 단순 모델) | 높음 (환율, 반도체지수, BOM 원가 등 활용) |
| 팀 기존 코드 | 없음 | `DB/07_pipeline/s4_forecast.py` 이미 구현됨 |
| 결론 | ❌ | ✅ 채택 |

#### 발견: 기존 파이프라인이 이미 완성되어 있었음
`DB/07_pipeline/` 폴더에 다음이 이미 구현되어 있었음:

```
s0_aggregation.py        ← 주별/월별 집계
s1_daily_inventory.py    ← 일간 추정 재고
s2_lead_time.py          ← 리드타임 통계
s3_feature_store.py      ← 피처 엔지니어링 (60개 피처)
s4_forecast.py           ← LightGBM P10/P50/P90 예측 (주간)
s4m_forecast_monthly.py  ← LightGBM 예측 (월간)
s5_risk_score.py         ← 리스크 스코어링
s6_action_queue.py       ← 조치 큐 생성
run_pipeline.py          ← 통합 실행기
```

**문제**: `feature_store_weekly` 테이블이 비어 있어서 s4가 실행되지 못하는 상태였음.
s0 → s1 → s2 → s3 의존 순서가 지켜지지 않아 발생.

---

### 2. ML 파이프라인 정확도 고도화

기존 코드의 한계점을 분석하고 5개 파일을 개선.

#### 개선 전/후 비교

| 항목 | 개선 전 | 개선 후 | 이유 |
|------|---------|---------|------|
| 하이퍼파라미터 탐색 | Grid Search (243 조합 고정) | **Optuna 베이지안 최적화** (100 trial) | 같은 시간에 더 넓은 공간 탐색, 스마트한 탐색 방향 |
| 과적합 방지 | 없음 (고정 n_estimators로만 학습) | **Early Stopping** 전면 적용 | 검증 손실이 나빠지면 자동 중단 |
| 평가 지표 | MAPE만 사용 | **WMAPE 추가** | 소량 제품에서 MAPE가 오차를 과장하는 문제 해결 |
| CV 폴드 수 | 주간 3-fold / 월간 2-fold | **주간 5-fold / 월간 3-fold** | 폴드 수가 적으면 평가 결과가 운에 따라 달라짐 |
| 리스크 등급 | A/B/C/D/F (E 없음) | **A/B/C/D/E/F** | E등급이 없어서 대시보드 "긴급 SKU" KPI가 항상 0이었던 버그 수정 |
| 샘플 제품 수 (튜닝) | 30개 | **50개** | 더 대표성 있는 샘플로 최적 파라미터 탐색 |

---

### 3. 변경 파일 상세

#### `DB/07_pipeline/ml_utils.py`

**변경 이유**: 모든 ML 모델이 공유하는 공용 유틸리티. 여기서 개선하면 주간/월간 모두 자동 적용.

| 함수 | 변경 내용 |
|------|-----------|
| `compute_metrics()` | WMAPE 계산 추가 |
| `_fit_lgb()` | 신규 — LightGBM 버전(4.x / 구버전) 모두 호환하는 Early Stopping 헬퍼 |
| `walk_forward_cv()` | `early_stopping_rounds` 파라미터 추가, `_fit_lgb` 사용으로 전환 |
| `grid_search_horizon()` | Early Stopping 파라미터 전달 지원 추가 |
| `optuna_search_horizon()` | **신규** — Optuna TPE 베이지안 최적화 (Grid Search 대체) |

**Optuna 탐색 파라미터 (9개):**
```
max_depth, learning_rate, num_leaves, min_child_samples,
reg_alpha (L1 정규화), reg_lambda (L2 정규화),
colsample_bytree, subsample, min_split_gain
```
> Grid Search는 선택한 값들의 조합만 탐색 → Optuna는 연속 공간을 이전 시도 결과를 바탕으로 스마트하게 탐색

---

#### `DB/07_pipeline/config.py`

| 상수 | 변경 전 | 변경 후 |
|------|---------|---------|
| `WEEKLY_CV_FOLDS` | 3 | **5** |
| `MONTHLY_CV_FOLDS` | 2 | **3** |
| `TUNE_SAMPLE_PRODUCTS` | 30 | **50** |
| `RISK_GRADE_BOUNDS` | A/B/C/D/F | **A/B/C/D/E/F** (E 추가) |
| `EARLY_STOPPING_ROUNDS` | 없음 | **50** (신규) |
| `OPTUNA_N_TRIALS` | 없음 | **100** (신규) |

**E등급 버그 수정 상세:**
```python
# 수정 전 (E등급 없음 → 대시보드 urgentCount = 항상 0)
RISK_GRADE_BOUNDS = [("A",20),("B",40),("C",60),("D",80),("F",100)]

# 수정 후
RISK_GRADE_BOUNDS = [("A",20),("B",40),("C",60),("D",75),("E",88),("F",100)]
```
대시보드 API(`route.ts`)가 E/F 등급 제품을 `urgentCount`로 집계하는데,
E등급이 생성되지 않아 항상 0이 반환되는 버그였음.

---

#### `DB/07_pipeline/s4_forecast.py` → MODEL_ID: `lgbm_q_v3`
#### `DB/07_pipeline/s4m_forecast_monthly.py` → MODEL_ID: `lgbm_q_monthly_v2`

공통 변경 사항:
- `n_estimators = 1000` (Early Stopping이 최적 트리 수 결정)
- `--tune` 실행 시 Optuna 우선 탐색, 미설치 시 Grid Search fallback
- CV + 최종 학습 모두 Early Stopping 적용
- `wmape` eval_rows에 포함

---

#### `DB/21_add_wmape_column.sql` (신규)
```sql
ALTER TABLE model_evaluation
    ADD COLUMN IF NOT EXISTS wmape NUMERIC(10,4);
```

---

### 4. Rule-based Baseline SQL 파이프라인 (전날 작업 보완)

`DB/20_baseline_pipeline.sql` — ML 파이프라인 실행 전 단계로 즉시 데이터 적재 가능:
- `generate_risk_score()` → `risk_score` 테이블
- `generate_action_queue()` → `action_queue` 테이블
- `generate_forecast_result()` → `forecast_result` 테이블 (이동평균 기반)
- `run_baseline_pipeline()` → 3개 함수 순서대로 실행

---

### 5. 대시보드 실데이터 전환 확인 (위험 도넛 / AI 생산 권고)

#### 확인 결과 — DB 이미 적재되어 있었음

| 테이블 | 행 수 | 최신 eval_date | 비고 |
|--------|------:|---------------|------|
| `risk_score` | 26,636 | 2026-03-01 | 등급 분포: A(771) B(122) C(106) D(1) |
| `action_queue` | 7,161 | 2026-03-01 | severity: critical(17) high(362) medium(121) |

#### 위험 도넛 차트 (`risk_score` 실데이터 전환)
- `Dashboard.tsx`: `riskGrades.length > 0` 이면 `✓ DB 실데이터` 배지(초록) 표시
- `riskGrades.length === 0` 이면 기존 `샘플` 배지(노랑) 유지 → 자동 fallback

#### AI 생산 권고 카드 (`action_queue` 실데이터 전환)
- `Dashboard.tsx`: `actionItems.length > 0` 이면 `✓ DB 실데이터` 배지(초록) 표시
- `actionItems.length === 0` 이면 기존 `샘플` 배지(노랑) 유지
- **버그 수정**: `action_type: "expedite_production"` 이 ACTION_TYPE_KO 매핑에 없어 원문 노출되던 문제 → `'긴급 생산 권고'` 추가
- **개선**: `product_master` 조인으로 product_id 대신 실제 `product_name` 표시

#### 수정 파일 요약
| 파일 | 변경 내용 |
|------|-----------|
| `forecastai/src/app/api/dashboard/route.ts` | `product_master` 조인 추가 (product_name 조회) / `expedite_production` 한글 매핑 추가 |
| `forecastai/src/components/pages/Dashboard.tsx` | 위험 도넛·AI 권고 각각 실데이터 배지 추가 |

---

### 6. 수주량 예측 밴드 실데이터 전환

> 전체 구역 최종 현황 및 연동 방식 상세는 → **섹션 9** 참고

#### 수주량 예측 밴드 문제 및 해결

**문제**: `ORDER_FORECAST` mock 월 범위 `'24.01~'25.03` ≠ DB 실적 `'25.01~'26.03` → 3개월만 매칭

**해결**: `route.ts` → `orderChartData` (통합 차트 데이터) 반환
- 최근 15개월 실적: `daily_order` 월별 집계
- 예측 밴드: `forecast_result` 최신 기준 전 제품 합산 (horizon 30→'26.04, 90→'26.06, 180→'26.09)
- `lastActualM`: ReferenceLine 동적 적용, `hasForecastData`: 예측 배지 플래그

#### 수정 파일
| 파일 | 내용 |
|------|------|
| `route.ts` | `forecast_result` 조회·집계, `orderChartData`/`lastActualM`/`hasForecastData` 반환 |
| `Dashboard.tsx` | `ChartRow` 타입, 예측 배지·ReferenceLine 동적화, `orderChartData` API 우선 사용 |

---

### 7. ML 파이프라인 실행 완료

#### 환경 세팅
- Python 버전: 3.14.2 (`py -3.14` 명령어 사용)
- 패키지 설치: `py -3.14 -m pip install lightgbm pandas numpy python-dotenv optuna "supabase==2.15.0"`
- `.env` 파일: 프로젝트 루트 (`PJTFNL/.env`) — `forecastai/.env.local`의 `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` 복사

#### ✅ ISS-002 해결 — supabase SDK 키 형식 패치

**오류 메시지 (해결 전)**:
```
supabase._sync.client.SupabaseException: Invalid API key
```

**원인**: `.env`의 `SUPABASE_SERVICE_ROLE_KEY = sb_secret_yNNcz...` → **신규 Supabase 키 형식** (`sb_secret_` prefix)
`supabase==2.7.4`는 구 형식(JWT, `eyJhbGci...` 시작)만 허용

**해결**: `supabase==2.15.0`으로 업그레이드 + `config.py`에 `SyncClient.__init__` 몽키패치 추가
- pyiceberg 의존성 없음 (빌드 오류 없음)
- `sb_` prefix 키 형식 우회 처리 완료
- 연결 테스트: `Supabase 연결 OK` 확인

#### 실행 결과

| 스텝 | 내용 | 상태 | 산출물 |
|------|------|------|--------|
| s0 | 주별·월별 집계 | ✅ 완료 | `weekly_product_summary`, `weekly_customer_summary` |
| s1 | 일간 추정 재고 | ✅ 완료 | `daily_inventory_estimated` |
| s2 | 리드타임 통계 | ✅ 완료 | `product_lead_time` |
| s3 | 피처 엔지니어링 (60개 피처) | ✅ 완료 | `feature_store_weekly` 83,719행 |
| s4 | LightGBM P10/P50/P90 학습 | ✅ 완료 | `forecast_result` 47,909건 신규 / 누적 265,082행<br>`model_evaluation` 4,280건, `feature_importance` 207건 |
| s5 | 리스크 스코어링 | ✅ 완료 | `risk_score` 39,931행 (A:10,074 / B:1,549 / C:1,565 / D:107 / E+F:26,636) |
| s6 | 조치 큐 생성 | ✅ 완료 | `action_queue` 10,613행 (critical:309 / high:3,062 / medium:81) |

> s4 소요 시간: 제품별 3 quantile × 5-fold CV → 약 1~2시간

---

### 8. 대시보드 UX 개선 — 주차 정보 + 헤더 알림 실데이터 전환

#### 8-1. 대시보드 타이틀 주차 정보 추가

**변경 파일**: `forecastai/src/components/pages/Dashboard.tsx`

```ts
// getWeekInfo() 헬퍼 추가 (파일 상단, 컴포넌트 외부)
function getWeekInfo(date: Date) {
  const d = new Date(date)
  const day = d.getDay() || 7        // 일=7, 월=1 (ISO 기준)
  d.setDate(d.getDate() - day + 1)   // 이번 주 월요일로 이동
  return {
    year:  d.getFullYear(),
    month: d.getMonth() + 1,
    week:  Math.ceil(d.getDate() / 7), // 월 내 주차 (1~5)
    start: d.toISOString().slice(0, 10),
    end:   (() => { d.setDate(d.getDate() + 6); return d.toISOString().slice(0, 10) })(),
  }
}
```

**결과**: 타이틀 아래 `2026년 3월 2주차 · 2026-03-09 ~ 2026-03-15 · 생산계획팀 주간 현황` 표시
- 매일 자동 갱신 (하드코딩 없음)
- 월요일 시작 기준, 월 내 몇 번째 주인지 한국어로 표시

---

#### 8-2. 헤더 알림 실데이터 전환

**배경**: 🔔 벨 아이콘 클릭 시 나타나는 드롭다운이 하드코딩된 Mock 데이터를 보여주고 있었음

**신규 API**: `forecastai/src/app/api/alerts/route.ts`

```
GET /api/alerts → { alerts: AlertItem[], source: 'database' }

AlertItem: { type: 'risk'|'warn'|'info', message: string, time: string }
```

**알림 생성 로직 (우선순위 순)**:
| 우선순위 | 소스 | 조건 | 알림 예시 |
|---------|------|------|---------|
| 1 | `action_queue` | severity='critical', status='pending' | "P-0042 — 긴급 생산 조치 필요" 🔴 |
| 2 | `risk_score` | risk_grade IN ('E','F'), 최신 eval_date | "위험 등급 E·F SKU 107건 — 즉시 조치 필요" 🔴 |
| 3 | `purchase_order` | status IN ('R','P') | "미처리 구매 발주 83건 — 입고 확인 필요" 🟡 |
| 4 | `action_queue` | severity='high', status='pending' | "P-0108 — 긴급 발주 권고" 🟡 (3건 미만 보충) |

**헤더 변경사항**: `forecastai/src/components/layout/index.tsx`
- 벨 클릭 시 최초 1회만 `/api/alerts` fetch → 이후 재클릭은 캐시 사용 (불필요한 API 반복 호출 방지)
- 로딩 중: "로딩 중..." 텍스트
- 데이터 없음: "알림 없음" 빈 상태
- 드롭다운 헤더에 "✓ DB 실데이터" 초록 배지 추가

---

### 9. 대시보드 전 구역 실데이터 연동 최종 완료 현황

> **팀원 공유용** — 2026-03-10 기준 모든 구역 실데이터 전환 완료

#### 프론트엔드 → 백엔드 → DB 흐름

```
브라우저 (Dashboard.tsx)
  ↓ fetch('/api/dashboard')
백엔드 (forecastai/src/app/api/dashboard/route.ts)
  ↓ Supabase RPC / REST 조회
DB (Supabase PostgreSQL)
```

#### 구역별 상세 연동 방식

**① 수주량 실적선 (주황 실선)**
```
daily_order 테이블 (259,684행)
  → get_order_monthly_summary(p_from_date) RPC [DB/23_order_monthly_rpc.sql]
  → 월별 합산 후 최대 15~18행만 반환 (limit=1000 문제 해결)
  → Dashboard: orderChartData[].actual
```

**② 수주량 예측 밴드 (파란 점선 + 음영)**
```
forecast_result 테이블 (265,082행, model: lgbm_q_v3)
  → 최신 forecast_date 1건 조회
  → get_forecast_summary(p_date) RPC [DB/22_dashboard_rpc.sql]
  → horizon_days(7/14/28)별 P10/P50/P90 합산 (product_id 중복 제거 후)
  → Dashboard: orderChartData[].p10 / p50 / p90, hasForecastData=true
```

**③ 재고 커버리지 KPI**
```
inventory 테이블 (617,720행) + daily_order 테이블
  → get_inventory_coverage(p_from_date, p_to_date) RPC [DB/24_coverage_rpc.sql]
  → 최근 30일 평균 일수요로 현재고 나누기 → 커버리지 일수 계산
  → Dashboard: inventoryCoverage.coverageDays
```

**④ 위험 도넛 차트 (A~F 등급)**
```
risk_score 테이블 (39,931행)
  → 최신 eval_date 조회 → get_risk_grade_summary(p_date) RPC
  → 등급별 SKU 수 카운트 (A:10,074 / B:1,549 / C:1,565 / D:107 / E+F:26,636)
  → Dashboard: riskGrades[] → donutData (빈 배열이면 Mock fallback)
```

**⑤ AI 생산 권고카드 Top3**
```
action_queue 테이블 (10,613행)
  → status='pending', severity IN ('critical','high','medium') 필터
  → severity 기준 상위 3건 조회
  → product_master 테이블 JOIN → product_name 표시 (product_id 대신)
  → Dashboard: actionItems[] (빈 배열이면 Mock fallback)
```

**⑥ 헤더 알림 🔔**
```
action_queue (critical) + risk_score (E/F등급) + purchase_order (미처리)
  → /api/alerts [forecastai/src/app/api/alerts/route.ts]
  → 벨 클릭 시 lazy fetch (최초 1회)
  → Header: alertItems[] (최대 5건)
```

**⑦ AI 인사이트 (GPT)**
```
weekly_product_summary 최근 2주 데이터 요약
  → OpenAI GPT-4o-mini 프롬프트
  → /api/ai-insights [forecastai/src/app/api/ai-insights/route.ts]
  → 6시간 서버 캐시 (과도한 API 비용 방지)
```

#### Supabase에서 실행 완료한 SQL 목록

| 파일 | 내용 | 실행 여부 |
|------|------|---------|
| DB/15_model_evaluation_ddl.sql | model_evaluation 테이블 생성 | ✅ |
| DB/21_add_wmape_column.sql | model_evaluation에 wmape 컬럼 추가 | ✅ |
| DB/22_dashboard_rpc.sql | get_forecast_summary, get_risk_grade_summary 함수 | ✅ |
| DB/23_order_monthly_rpc.sql | get_order_monthly_summary 함수 | ✅ |
| DB/24_coverage_rpc.sql | get_inventory_coverage 함수 | ✅ |

#### Mock Fallback 전략
- DB 데이터 없거나 ML 미실행 → 각 구역 자동으로 Mock 데이터 표시 + "샘플" 노랑 배지
- DB 실데이터 있으면 → "✓ DB 실데이터" 초록 배지 자동 전환
- 코드 변경 없이 DB 상태만으로 자동 전환 (안전한 점진적 적용)

---

## 내일 할 일 (2026-03-11)

### 확인 사항
- [ ] Supabase SQL Editor에서 현재 MAPE 확인:
  ```sql
  SELECT model_id, horizon_days, ROUND(AVG(mape)::numeric, 2) AS avg_mape,
         ROUND(AVG(wmape)::numeric, 2) AS avg_wmape
  FROM model_evaluation
  WHERE model_id = 'lgbm_q_v3'
  GROUP BY model_id, horizon_days ORDER BY horizon_days;
  ```
  → avg_mape ≥ 30% 이면 Optuna 튜닝 검토
- [ ] 대시보드 전체 구역 육안 확인 (배지 색상 초록인지)
- [ ] 헤더 알림 벨 클릭 → 실데이터 알림 표시 확인

### Optuna 튜닝 (선택 — MAPE ≥ 30% 일 때만)
```bash
cmd /c "py -3.14 DB/07_pipeline/run_pipeline.py --step=4 --tune"
```
> 소요 시간: 2~4시간 이상. 기존 데이터 유지됨(새 forecast_date로 추가).

### 다음 개발 작업
- [ ] 관리자 페이지 실데이터 연동 (user_profile 테이블)
- [ ] Supabase: `ALTER TABLE user_profile ADD COLUMN IF NOT EXISTS org_id VARCHAR(100) DEFAULT 'default';`
- [ ] 주간 리포트 상세 페이지 UI 연결 (`/api/weekly-report` 이미 완성됨)

---

## 이슈 / 특이사항

- ~~ISS-002: supabase SDK 신규 키 형식 미지원~~ → **해결 완료** (섹션 7 상세 참고)
- Python 실행은 `py -3.14` (Windows Python Launcher 방식)
- 파이프라인 루트 `.env` 필요: `PJTFNL/.env` (forecastai/.env.local과 별개)
- 패키지 버전 확정: lightgbm 4.6.0, pandas 3.0.1, numpy 2.4.3, optuna 4.7.0, supabase 2.15.0, scikit-learn 1.8.0

---

*문의: rusia0567@naver.com / dev/dasom 브랜치*
