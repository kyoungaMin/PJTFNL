# KoraenDavid 개발 일지 - 2026-03-09

## 🚀 1. GitHub 인증 및 Push 환경 구성
*   **문제**: KoraenDavid 브랜치에서 로컬 커밋 내역(Inventory.tsx 일부 작업)을 푸시하려 했으나, GitHub SSH/HTTPS 쓰기 권한 이슈 발생.
*   **해결 및 진행**:
    *   기존 원격 브랜치의 변경 내역과 병합(Merge) 충돌을 해결 (`Inventory.tsx` 등).
    *   인증 이슈 점검 및 협업자(Collaborator) 권한 문제 확인.

## ⚡ 2. `Inventory.tsx` 렌더링 최적화
*   **문제**: 수천 단위가 넘는 SKU 데이터를 한 번에 HTML 테이블로 렌더링하려고 하면서, 탭을 이동하거나 검색/필터를 적용할 때 브라우저 렉(응답 없음) 현상 발생.
*   **해결 내역**:
    *   `visibleCount` 상태(State)를 도입하여 **페이지네이션(Pagination)** 형태로 50개 항목만 최초로 표시하도록 수정.
    *   최하단에 **"더보기"** UI 컴포넌트를 추가하여 버튼 클릭 시 추가로 50개씩 뷰에 추가되도록 점진적 로딩 처리 보강.
    *   `useMemo`를 통한 정렬/필터링 캐시로 재렌더링 시 연산 과부하 문제 예방.

## ⚡ 3. 백엔드 API (`route.ts`) 동시 처리 최적화
*   **문제**: 프론트엔드 최적화 이후에도 대시보드 모드(`mode: dashboard`)로 데이터를 가져올 때 로딩이 지연되는 문제 확인. `risk_score` / `weekly_product_summary` / `purchase_order` / `weekly_customer_summary` 등 각 지표를 조회할 때 `await`가 직렬로 배치되어 속도 병목 초래.
*   **해결 내역**:
    *   `Promise.all` 메서드를 사용하여 독립적인 Supabase 원격 테이블 조회를 전부 병렬 쿼리로 전환 배치 (1/5 속도로 단축).
